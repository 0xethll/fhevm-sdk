{
  "name": "fhevm-sdk-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Universal FHEVM SDK for building confidential dApps",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "overrides": {
    "wagmi": "^2.0.0",
    "viem": "^2.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
